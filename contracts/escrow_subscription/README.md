# KethyrPay Escrow Subscription POC

Proof-of-concept Leo smart contract for the KethyrPay escrow subscription module.

## Project Purpose

This repository contains the initial Leo program scaffolding for the escrow subscription functionality of KethyrPay. The goal is to provide a minimal, buildable Leo project that serves as the foundation for subscription-based escrow logic on the Aleo network.

## Requirements

- [Leo CLI](https://docs.aleo.org/build/leo) >= v4.0.0

Install or upgrade the Leo CLI:

```bash
leo update
```

## Build Command

```bash
cd escrow_subscription
leo build
```

## Test Command

```bash
cd escrow_subscription
leo test
```

## Testnet Deployment Command Template

```bash
cd escrow_subscription
leo deploy --network testnet
```

## Directory Structure

```
contracts/escrow_subscription/
├── escrow_subscription/          # Leo program package generated by `leo new`
│   ├── src/
│   │   └── main.leo              # Program source code (Leo language)
│   ├── tests/
│   │   └── ...                   # Leo test programs
│   ├── program.json              # Program metadata
│   └── README.md                 # Auto-generated Leo build guide
├── README.md                     # Project documentation
└── .env.example                  # Environment variable placeholders
```

## Environment Variables

Copy `.env.example` to `.env` and fill in your values before deploying.
